#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../testify/bin/testify" "$@"
  ret=$?
else 
  coffee  "$basedir/../testify/bin/testify" "$@"
  ret=$?
fi
exit $ret
